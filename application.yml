spring:
  datasource:
    url: jdbc:mysql://localhost:3306/asset_management
    username: root
    password: 0000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  server:
    port: 8080