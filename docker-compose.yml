services:
  spring-deploy-student8-app:
    build:
      context: Final Project
      dockerfile: Final Project/Dockerfile
    image: ${DOCKERHUB_USERNAME}/spring-deploy-student8-app:latest
    container_name: spring-deploy-student8-app
    ports:
      - ${APP_PORT:-9008}:${APP_PORT:-9008}
    env_file:
      - .env
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:$${APP_PORT:-9008}/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - classroom_classroom_net

networks:
  classroom_classroom_net:
    external: true
